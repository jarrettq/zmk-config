# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
include:
  # Left peripheral (forced via cmake-args)
  - board: nice_nano
    shield: corne_left nice_view_adapter nice_view
    cmake-args: -DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
  
  # Right peripheral (forced via cmake-args)
  - board: nice_nano
    shield: corne_right nice_view_adapter nice_view
    cmake-args: -DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
  
  # Dongle central with prospector display
  - board: seeeduino_xiao_ble
    shield: corne_dongle prospector_adapter
  
  # Settings reset firmware (flash after main firmware)
  - board: nice_nano
    shield: settings_reset
  - board: seeeduino_xiao_ble
    shield: settings_reset
